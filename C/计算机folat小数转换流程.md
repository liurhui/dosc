浮点数

```c
整数部分 和负数部分
    负数 2^-1 =0.5
        2^-3 =0.125 
    组合起来是 0.625
    
以 10.625 为例    
符号位		  指数		  尾数
0表示＋	   8位			23位
2表示-    10000010      0101 010.....0  //正数 0   后面数是 二进制的10.625  不足的补0
    
  在小数点开始 1010.101  小数点不动数右移  变成1.010101   小数点左移了3位 即+3
    3+127 =130   130 用二进制表示 是 128+2  1000 0010   也就成了指数的值

    这里说的左移右移  以 数字为对象来移动    如果 以小数点作为移动对象  那么 就是 右移左移  
    
    数字为对象移动 就是 数字在移动  小数点不动 看起来就是 最大值是 左移    最小值右移 
    
folat 最大值和最小值
 0 8个1    23个1   但是 这个数是保留的给系统
    系统给的 最大值是 左移127位   最小值右移126位
8个1 是移动了128位 128+127 正好=255     
    所以最大值应该是 128 减去1 
也就是 0 7个1  0   23个1    这样换出来就是最大值    3.4028235E38
 
    这里的最小值有两个 意思 实际中 float 表示的最小值是 无限接近0 但比0大的一个数 精度的问题
    另一个就是 负的flaot 的最大值也就是 最小值 

 正的最小 也就是 0  7个0 1  23个0   但是这里有个问题 
    当右移了126 这个时候 这个1实际上 没到1还是接近0 那么会出现 0.23个0 1  称为min_value
    1.23个0   称为 min_normal

 mini_value =0x0.000002p-126f  这里的2是 因为十六进制表示1位等于4位 2是因为本来要24位凑齐的 这里这有23位 所以本来在8421 中的bit0位也就是1  被强制的变成2了    
    
    在float里面 有精度问题是因为很多数不是 2的次方 那么这样的数只能够用无限接近这个数来表达 所以会有误差
    
```

